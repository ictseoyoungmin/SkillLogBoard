---
week: 2
day: 5
slice: "01_basic_usage_example_update"
title: "basic_usage example update"
priority: "P0"
status: "completed"
target_version: "v0.1-mvp"
---

# 01_basic_usage_example_update — basic_usage example update

## Objective

Update `examples/basic_usage.py` to demonstrate the v0.1 MVP logger flow.

## Context

The Week 2 completion gate requires a real basic usage example that creates a complete run evidence package.

## Dependencies

- week2/day1-day4 slices

## Target Files

- examples/basic_usage.py
- README.md

## Implementation Steps

1. Update the example to instantiate RunLogger with realistic config fields.
2. Log several train/validation metrics across multiple steps.
3. Log at least one note.
4. Log at least one small artifact file generated by the example.
5. Call `finish(build_dashboard=True, build_report=True)`.
6. Print the generated run directory path at the end.
7. Update README to reference this example.

## Acceptance Criteria

- `python examples/basic_usage.py` runs from the repository root.
- The generated run contains manifest, config, system, git, metrics, events, summary, dashboard, and artifact output.
- The example does not require torch, pandas, or internet access.

## Verification Commands

```bash
python examples/basic_usage.py
```

## Non-goals

- Do not include framework-specific training in `basic_usage.py`.

## Handoff Notes

- Keep this slice focused on Week 2 MVP behavior.
- Preserve the public API described in the docs unless this slice explicitly changes it.
- Prefer backward-compatible changes to the Week 1 skeleton.
- Do not start Week 3 dashboard work beyond the placeholder hooks required by `finish()`.
- If a blocker appears, document it in the Agent Completion Block instead of expanding scope.

---

## Agent Completion Block

> The agent must update this block after finishing the slice.

- [x] Implementation completed
- [x] Acceptance criteria verified
- [x] Tests or smoke checks executed
- [x] No unrelated files changed
- [x] Notes added below if anything was skipped or deferred

**Status:** COMPLETED  
**Completed at:** 2026-05-09 22:20  
**Completed by:** coding agent  
**Verification command(s):** python examples/basic_usage.py  
**Notes:** Completed for Week 2 v0.1 MVP scope only.  

<!-- AGENT_STATUS: COMPLETED -->
