"""Synthetic example scaffold for {template_name}."""

from __future__ import annotations

from skilllogboard import RunLogger


def main() -> None:
    # TODO: replace with deterministic synthetic data generation for this template.
    logger = RunLogger(
        project="{slug}_demo",
        run_name="synthetic-{slug}",
        root_dir="runs/{slug}_demo",
        config={default_config_repr},
        main_metric={{"name": "{first_metric}", "mode": "max"}},
    )
    for step in range(3):
        logger.log_metric("{first_metric}", 0.5 + step * 0.1, step=step)
    logger.finish()
    print(f"Run written to: {{logger.run_dir}}")


if __name__ == "__main__":
    main()
