experiment:
  name: residual_knn_cv_last1
  seed: 20260517
  tags: [residual, knn, non-tree]
paths:
  data_root: data
  results_root: results
cv:
  n_splits: 5
model:
  type: residual
  base_method: cv_last1
  model_kind: knn
  params:
    n_neighbors: 80
    weights: distance
    p: 2
submission:
  threshold: 0.7000
